# Core (required to run the algorithmic reproduction + experiments)
numpy>=1.24
matplotlib>=3.7

# Testing
pytest>=7.4

# --- Optional extras (uncomment to enable) ---
# Real semantic embeddings instead of the offline hashing embedder:
# sentence-transformers>=2.2
# Real GPT-4o diary text + DALL·E 3 images (requires OPENAI_API_KEY):
# openai>=1.30
# Chroma vector-store backend:
# chromadb>=0.5
